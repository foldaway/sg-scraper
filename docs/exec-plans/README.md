# Execution Plans

Store active plans in `active/` and finished plans in `completed/`.

Plans are for work that needs durable state across agent runs. Small changes can
use the conversational plan only.
